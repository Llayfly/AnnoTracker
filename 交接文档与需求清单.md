# 标注工作台监控系统 — 交接文档与需求清单

> 本文件供后续接手开发的 AI 使用。包含：项目概况、已完成功能、待办需求、**已探测到的真实 API 接口结构与数据字段**（这部分是排查接口得到的宝贵信息，勿重复探测）、状态与审核结果枚举映射、部署方式。

***

## 一、项目概况

* **项目名称**：AnnoTracker（标注员工作统计监控系统）

* **位置**：`/workspace/annotator-stats`

* **技术栈**：Node.js + Express（无框架，使用 Vercel Serverless Functions `api/*.js`）+ 原生 HTML/CSS/JS 前端 + JWT 登录（localStorage 存 token）

* **部署**：Vercel，生产域名 `https://anno.llayfly.me`，GitHub 仓库 `https://github.com/Llayfly/AnnoTracker.git`

* **数据来源**：`https://data-platform.synapath.com`（标注平台后端 REST API）

* **登录账号**（监控网站访问凭据）：见 `.env` / `lib/auth.js`（站点访问密码）；平台对接账号见 `.env`（`PLATFORM_EMAIL` / `PLATFORM_PASSWORD`）

### 目录结构

```
api/batches.js        批次管理接口（真实 API 已打通）
api/stats.js          时长监控统计接口（聚合多个日期的平台数据）
api/stats/[label].js  单个标注员每日详情
api/check-auth.js     校验登录 token
api/login.js / logout.js
api/health.js         平台连接健康检查
api/collect.js        数据采集（本地版用，Vercel 上可由 stats 实时聚合替代）
api/logs.js
lib/platformApi.js    平台 API 客户端（登录、拿 token、批量拉取每日统计）
lib/auth.js           网站登录认证
public/index.html     时长监控页
public/batches.html   批次管理页
public/js/app.js      时长监控前端逻辑（柱状图渲染）
public/css/style.css  全局样式
public/login.html     登录页
vercel.json           Vercel Functions 配置
package.json          依赖（node-fetch）
```

***

## 二、已完成功能

1. **时长监控页**（`/`）

   * 顶部导航 + 横幅

   * 时间范围选择：最近1天 / 3天 / 1周 / 半个月 / 1月 / 自定义日期区间

   * 搜索标注员关键词

   * 按组织分组（前缀如 HC / HBHC / C）的横向柱状图

   * 柱状图蓝色 = 新增任务，黄色 = 旧任务，柱内及右侧显示各自时长

   * 预警边框：日均 <3h 红 / 3\~5h 蓝 / >5h 绿

   * 点击柱状图弹窗显示该标注员每日明细（每日叠加柱：蓝新增 + 黄旧）

   * 导出 CSV

   * 每 10 分钟自动刷新
2. **批次管理页**（`/batches.html`）

   * 通视频次的真实 API（`/api/batches`）

   * 表格列出批次数据
3. **登录页**：用户名密码 + 密码可见性切换
4. **Vercel 部署**：已上线 `https://anno.llayfly.me`，GitHub 自动构建

***

## 三、用户最新提出的调整需求（尚未完成，需实现）

用户反馈（原话整理），以下为重点待办：

1. **批次管理表格太丑，且出现英文**（平台返回英文状态如 `dispatched`，需要转成中文）。
2. **批次管理页要加筛选 + 查询功能**（状态筛选、项目筛选、关键词搜索）。
3. **批次表格列要好看**，符合用户提供的参考图。
4. **状态有四种**：`通过`、`需修改`、`待审核`、`已分发`（结合真实数据还有 `预审核通过`）。

   * **只有"需修改"状态的批次才需要修改处理**，此状态要突出提示。
5. **供应商列不需要**（从表格去掉）。
6. **导航结构调整**：把"时长监控"和"批次管理"放到一个叫"统计监控"的统一入口/标题下面（即二级导航，父级为"统计监控"）。
7. **时长要写进柱状图里面**（用户反馈当前柱状图里没显示时长数字）。务必保证新任务时长（蓝）和旧任务时长（黄）的数值在柱状图上清晰可见。

***

## 四、已探测到的平台 API（勿重复探测，直接使用）

> 平台 Base URL：`https://data-platform.synapath.com`

### 1. 登录获取 token

* `POST /api/v1/annotator-auth/login`

* Body：`{ "email": "...", "password": "..." }`

* 响应：`{ "access_token": "...", "expires_in": 28800, "email": "..." }`

### 2. 批次列表（重点）

* `GET /api/v1/dispatch/batches?limit=200&organization=HC`

* **支持参数**（实测）：`limit`、`organization`、`status`、`annotator`、`label_project_id`

* **响应结构**：

```json
{
  "batches": [ { ...批次对象... } ],
  "pagination": { "page": 1, "total": 2538, "size": 200 }
}
```

#### 批次对象字段（真实字段名，必须用这些）

| JSON 字段               | 含义                                      |
| --------------------- | --------------------------------------- |
| `id`                  | 批次ID                                    |
| `label_project_id`    | 标注项目ID                                  |
| `label_project_name`  | 标注项目名称（如"生产制造类标注"）                      |
| `provider_names`      | 供应商数组（**用户要求去掉**）                       |
| `task_combinations`   | 任务组合数组（如 \["生产制造类 / 包装车间 / 混合任务"]）      |
| `created_by_name`     | 创建人/分配人                                 |
| `annotator_name`      | 标注员编码（如 HC29）                           |
| `status`              | 状态（枚举见下）                                |
| `decision`            | 审核结果（枚举见下，未审核为 null）                    |
| `decision_note`       | 审核备注                                    |
| `review_round`        | 轮次数字                                    |
| `reviewed_by_name`    | **实际审核人**（预审核/审核后非空，如 SH26）             |
| `reviewer_name`       | 分配审核人（可能为 null）                         |
| `ready_for_review_at` | 提交待审核时间                                 |
| `approved_at`         | 通过时间                                    |
| `created_at`          | 创建时间（ISO，如 2026-09-04T13:33:03.856145Z） |

#### status 状态枚举（实测）

| 值                  | 中文映射  |
| ------------------ | ----- |
| `dispatched`       | 已分发   |
| `ready_for_review` | 待审核   |
| `pre_approved`     | 预审核通过 |
| `approved`         | 通过    |
| `needs_changes`    | 需修改   |

#### decision 审核结果枚举（实测）

| 值               | 中文映射       |
| --------------- | ---------- |
| `approve`       | 通过         |
| `needs_changes` | 需修改        |
| `null`          | 未审核（显示"-"） |

### 3. 每日统计（时长监控数据源）

* `GET /api/v1/analytics/annotation-analytics?organization=HC&day=YYYY-MM-DD`

* 响应关键字段：

  * `selected_day`、`selected_organization`

  * `organization_person_daily_rows[]`：

    * `person_label`（标注员，取第一个空格前的部分作为唯一标识）

    * `raw_video_duration_seconds`（原始时长秒）

    * `segment_duration_seconds`（片段时长秒）

    * `no_clip_duration_seconds`（无片段时长秒）

    * `new_task_raw_video_duration_seconds`（**新增任务**原始时长秒）

    * `old_task_raw_video_duration_seconds`（**旧任务**原始时长秒）

  * `organization_person_settlement_rows[]`：结算参考

  * `no_clip_equivalent_factor`（默认 0.2）

* **时长基准**：1 天 = 86400 秒，换算小时 = 秒 / 3600。

* **逻辑**：蓝色(新增) = 当天新提交批次时长；黄色(旧) = 当天被打回又改好的旧批次时长。日报聚合时把多天结果按标注员累加，汇总 `total_new_task_hours` / `total_old_task_hours` / `total_raw_hours` 等。

***

## 五、建议的批次表格列（按用户参考图，去掉供应商）

| 列     | 来源字段                   | 说明                    |
| ----- | ---------------------- | --------------------- |
| 批次ID  | `id`                   | 等宽字体                  |
| 标注项目  | `label_project_name`   | <br />                |
| 任务组合  | `task_combinations[0]` | 可能较长，截断显示             |
| 状态    | `status`               | 中文 + 彩色标签             |
| 审核结果  | `decision`             | 中文                    |
| 实际审核人 | `reviewed_by_name`     | 为空显示"-"               |
| 标注员   | `annotator_name`       | <br />                |
| 轮次    | `review_round`         | <br />                |
| 创建时间  | `created_at`           | 格式化为 YYYY/MM/DD HH:MM |

筛选：状态下拉（全部/已分发/待审核/预审核通过/通过/需修改）+ 标注项目下拉 + 搜索框（匹配批次ID或标注员）。"需修改"状态建议红字/红底标签突出。

***

## 六、部署方式

* 推送 Git：`git add -A && git commit -m "..." && git push origin master`

* Vercel 已绑定 GitHub 仓库，推 master 自动构建。

* 若需手动部署：`npx vercel --prod --token <TOKEN>`（当前仓库已 `vercel link` 到 `llay/anno-tracker`）。

* 生产域名：`https://anno.llayfly.me`

* **注意**：Vercel Functions 冷启动时会用 `lib/platformApi` 实时调平台登录 + 拉数据，`.env` 中平台凭据需在 Vercel 环境变量中配置（`PLATFORM_BASE_URL` / `PLATFORM_EMAIL` / `PLATFORM_PASSWORD` / `PLATFORM_ORGANIZATION`）。

***

## 七、易踩坑提示

1. 平台 API 返回的时间是 UTC ISO，前端 `new Date()` 显示直接本地化，`toLocaleString('zh-CN')` 即可。
2. 标注员标识 `person_label` / `annotator_name` 可能带后缀，统一取第一个空格前部分（`cleanLabel` 函数已处理）。
3. 柱状图时长标签若柱宽过窄会被裁切，务必在柱内加 `min-width` + 让标签白字阴影，或在行右侧固定显示数值（推荐两者都做，保证"时长写进柱状图"）。
4. 批次接口最多返回 `limit` 条；用户量 2538 条，分页 `pagination.page/total/size`。当前只用前 200 条可接受，若要全量需做分页循环。

